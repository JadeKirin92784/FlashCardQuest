<h1>Let's make a new deck! <h1>
<%= simple_form_for @deck do |f| %>
 <%= f.input :topic, label: "Enter the topic or name of your new deck" %>   
 <%= f.button :submit, "Make new deck!" %> 
<% end %>

<%= link_to "Add a card",  new_flashcard_path %>
